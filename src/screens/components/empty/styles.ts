import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 30,
    marginHorizontal: 26,
  },
  description: {
    marginTop: 20,
    color: '#808080',
    fontSize: 17,
    textAlign: 'center',
  }
});